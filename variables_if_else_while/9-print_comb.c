/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

/* betty style doc for function main goes there */
int main(void)
{
        int i,
	
	for (i = 0; i < 9; i++)
		if ( i < 9)
		{
			printf("%d ", i);
		}
		else
		{
			printf("%d", i);
		}
		return (0);
}
